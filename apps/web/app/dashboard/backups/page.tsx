"use client";
import { useEffect, useState } from "react";
import { getAccessToken } from "@/lib/auth";
const apiUrl = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";
type Backup = { id:string; status:string; filePath?:string; fileSize:number; userEmail?:string; message?:string; createdAt:string };
export default function BackupsPage(){const[items,setItems]=useState<Backup[]>([]);const[loading,setLoading]=useState(true);const[message,setMessage]=useState("");useEffect(()=>{void load();},[]);async function load(){setLoading(true);const response=await fetch(`${apiUrl}/backups`,{headers:{Authorization:`Bearer ${getAccessToken()}`}});if(response.ok)setItems(await response.json());setLoading(false);}async function createBackup(){setMessage("");const response=await fetch(`${apiUrl}/backups/export`,{method:"POST",headers:{Authorization:`Bearer ${getAccessToken()}`}});if(response.ok){setMessage("Sauvegarde preparee avec succes.");await load();}else setMessage("Impossible de preparer la sauvegarde.");}return <div className="space-y-5"><div className="flex flex-col justify-between gap-4 rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 md:flex-row md:items-center"><div><p className="text-sm font-medium text-brand-600">Sauvegarde</p><h1 className="text-2xl font-bold text-slate-950 dark:text-white">Sauvegardes</h1><p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Historique des sauvegardes PostgreSQL preparees.</p></div><button onClick={()=>void createBackup()} className="rounded-md bg-brand-600 px-4 py-2 text-sm font-semibold text-white">Creer sauvegarde</button></div>{message?<div className="rounded-lg border border-slate-200 bg-white p-4 text-sm dark:border-slate-800 dark:bg-slate-900">{message}</div>:null}<div className="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-slate-500 dark:bg-slate-950"><tr><th className="p-3">Date</th><th className="p-3">Statut</th><th className="p-3">Utilisateur</th><th className="p-3">Taille</th><th className="p-3">Chemin</th><th className="p-3">Message</th></tr></thead><tbody>{items.map((item)=><tr key={item.id} className="border-t border-slate-100 dark:border-slate-800"><td className="p-3">{new Date(item.createdAt).toLocaleString("fr-HT")}</td><td className="p-3">{item.status}</td><td className="p-3">{item.userEmail??"--"}</td><td className="p-3">{item.fileSize} octets</td><td className="p-3 font-mono text-xs">{item.filePath??"--"}</td><td className="p-3">{item.message??"--"}</td></tr>)}</tbody></table>{!loading&&!items.length?<div className="p-6 text-sm text-slate-500">Aucune sauvegarde.</div>:null}{loading?<div className="p-6 text-sm text-slate-500">Chargement...</div>:null}</div></div>}