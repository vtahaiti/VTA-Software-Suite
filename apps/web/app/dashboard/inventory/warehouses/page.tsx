"use client";
import { apiBaseUrl as apiUrl } from "@/lib/api-url";
import { FormEvent,useEffect,useState } from "react";
import { getAccessToken } from "@/lib/auth";
export default function WarehousesPage(){const[items,setItems]=useState<any[]>([]);const[form,setForm]=useState({name:"",code:"",address:""});useEffect(()=>{load()},[]);async function load(){const r=await fetch(`${apiUrl}/warehouses`,{headers:{Authorization:`Bearer ${getAccessToken()}`}});if(r.ok)setItems(await r.json())}async function submit(e:FormEvent){e.preventDefault();const r=await fetch(`${apiUrl}/warehouses`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${getAccessToken()}`},body:JSON.stringify(form)});if(r.ok){setForm({name:"",code:"",address:""});load()}}return <div className="grid gap-6 lg:grid-cols-[360px_1fr]"><section className="rounded-lg border bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><h1 className="text-2xl font-bold">Entrepots</h1><form onSubmit={submit} className="mt-4 space-y-3"><input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="Nom" className="w-full rounded-md border px-3 py-2 dark:bg-slate-950"/><input value={form.code} onChange={e=>setForm({...form,code:e.target.value})} placeholder="Code" className="w-full rounded-md border px-3 py-2 dark:bg-slate-950"/><input value={form.address} onChange={e=>setForm({...form,address:e.target.value})} placeholder="Adresse" className="w-full rounded-md border px-3 py-2 dark:bg-slate-950"/><button className="w-full rounded-md bg-brand-600 px-4 py-2 text-white">Creer</button></form></section><section className="grid gap-3 md:grid-cols-2">{items.map(x=><div key={x.id} className="rounded-lg border bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><p className="font-semibold">{x.name}</p><p className="text-sm text-slate-500">{x.code}</p><p className="text-sm">{x.address}</p></div>)}</section></div>}