"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
import { getAccessToken } from "@/lib/auth";
const apiUrl=process.env.NEXT_PUBLIC_API_URL??"http://localhost:3001";
export default function CreateStorePage(){const router=useRouter();const[form,setForm]=useState({code:"",name:"",phone:"",email:"",country:"HT",city:"",address:""});const[error,setError]=useState("");async function submit(){setError("");const r=await fetch(`${apiUrl}/stores`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${getAccessToken()}`},body:JSON.stringify(form)});if(r.ok)router.push("/dashboard/stores");else setError("Creation magasin impossible.");}return <div className="mx-auto max-w-3xl space-y-5"><div className="rounded-lg border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900"><p className="text-sm font-medium text-brand-600">Magasins</p><h1 className="text-2xl font-bold">Nouveau magasin</h1></div>{error?<div className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</div>:null}<div className="grid gap-4 rounded-lg border border-slate-200 bg-white p-5 dark:border-slate-800 dark:bg-slate-900 md:grid-cols-2">{Object.keys(form).map((key)=><label key={key} className="grid gap-1 text-sm font-medium capitalize">{key}<input value={(form as any)[key]} onChange={(e)=>setForm({...form,[key]:e.target.value})} className="rounded-md border px-3 py-2 dark:border-slate-700 dark:bg-slate-950"/></label>)}<button onClick={()=>void submit()} className="rounded-md bg-brand-600 px-4 py-2 text-sm font-semibold text-white md:col-span-2">Creer magasin</button></div></div>}