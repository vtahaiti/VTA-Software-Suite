"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { getAccessToken } from "@/lib/auth";
const apiUrl=(process.env.NEXT_PUBLIC_API_URL ?? (process.env.NODE_ENV === "production" ? "https://api.vtaerp.com" : "http://localhost:3001"));
type Store={id:string;code:string;name:string;phone?:string;city?:string;status:string;warehouses?:unknown[]};
export default function StoresPage(){const[items,setItems]=useState<Store[]>([]);const[search,setSearch]=useState("");useEffect(()=>{void load();},[]);async function load(){const params=new URLSearchParams();if(search)params.set("search",search);const r=await fetch(`${apiUrl}/stores?${params}`,{headers:{Authorization:`Bearer ${getAccessToken()}`}});if(r.ok){const d=await r.json();setItems(d.items??[])}}async function close(id:string){await fetch(`${apiUrl}/stores/${id}/close`,{method:"PATCH",headers:{Authorization:`Bearer ${getAccessToken()}`}});await load();}return <div className="space-y-5"><div className="flex flex-col justify-between gap-4 rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 md:flex-row md:items-center"><div><p className="text-sm font-medium text-brand-600">Enterprise</p><h1 className="text-2xl font-bold text-slate-950 dark:text-white">Magasins</h1><p className="text-sm text-slate-500">Gérez les magasins, dépôts, stock et ventes par site.</p></div><Link href="/dashboard/stores/create" className="rounded-md bg-brand-600 px-4 py-2 text-sm font-semibold text-white">Nouveau magasin</Link></div><div className="flex gap-2 rounded-lg border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><input value={search} onChange={(e)=>setSearch(e.target.value)} placeholder="Recherche" className="flex-1 rounded-md border px-3 py-2 dark:border-slate-700 dark:bg-slate-950"/><button onClick={()=>void load()} className="rounded-md bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-white dark:text-slate-950">Filtrer</button></div><div className="overflow-hidden rounded-lg border border-slate-200 bg-white dark:border-slate-800 dark:bg-slate-900"><table className="w-full min-w-[760px] text-left text-sm"><thead className="bg-slate-50 text-slate-500 dark:bg-slate-950"><tr><th className="p-3">Code</th><th className="p-3">Magasin</th><th className="p-3">Ville</th><th className="p-3">Téléphone</th><th className="p-3">Dépôts</th><th className="p-3">Statut</th><th className="p-3">Actions</th></tr></thead><tbody>{items.map((s)=><tr key={s.id} className="border-t border-slate-100 dark:border-slate-800"><td className="p-3 font-mono text-xs">{s.code}</td><td className="p-3 font-semibold">{s.name}</td><td className="p-3">{s.city??"--"}</td><td className="p-3">{s.phone??"--"}</td><td className="p-3">{s.warehouses?.length??0}</td><td className="p-3">{s.status}</td><td className="p-3"><button onClick={()=>void close(s.id)} className="text-red-600">Fermer</button></td></tr>)}</tbody></table>{!items.length?<div className="p-6 text-sm text-slate-500">Aucun magasin.</div>:null}</div></div>}
