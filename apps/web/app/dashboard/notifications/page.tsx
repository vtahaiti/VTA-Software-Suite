"use client";
import { useEffect, useMemo, useState } from "react";
import { getAccessToken } from "@/lib/auth";
const apiUrl = process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:3001";
type Notification = { id:string; title:string; message:string; type:string; status:string; module?:string; referenceId?:string; createdAt:string; readAt?:string };
const statusOptions=[{value:"",label:"Toutes"},{value:"unread",label:"Non lues"},{value:"read",label:"Lues"},{value:"archived",label:"Archivees"}];
const typeOptions=[{value:"",label:"Tous types"},{value:"info",label:"Info"},{value:"success",label:"Succes"},{value:"warning",label:"Alerte"},{value:"error",label:"Erreur"}];
export default function NotificationsPage(){const[items,setItems]=useState<Notification[]>([]);const[count,setCount]=useState(0);const[status,setStatus]=useState("unread");const[type,setType]=useState("");const[moduleName,setModuleName]=useState("");const[message,setMessage]=useState("");const[loading,setLoading]=useState(true);const token=useMemo(()=>getAccessToken(),[]);useEffect(()=>{void load();},[status,type,moduleName]);async function load(){if(!token)return;setLoading(true);const params=new URLSearchParams();if(status)params.set("status",status);if(type)params.set("type",type);if(moduleName)params.set("module",moduleName);const headers={Authorization:`Bearer ${token}`};const[itemsResponse,countResponse]=await Promise.all([fetch(`${apiUrl}/notifications?${params}`,{headers}),fetch(`${apiUrl}/notifications/unread-count`,{headers})]);if(itemsResponse.ok)setItems(await itemsResponse.json());if(countResponse.ok)setCount(((await countResponse.json()) as {count:number}).count);setLoading(false);}async function patch(path:string,success:string){if(!token)return;setMessage("");const response=await fetch(`${apiUrl}${path}`,{method:"PATCH",headers:{Authorization:`Bearer ${token}`}});setMessage(response.ok?success:"Action impossible.");await load();}return <div className="space-y-5"><div className="flex flex-col justify-between gap-4 rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900 md:flex-row md:items-center"><div><p className="text-sm font-medium text-brand-600">Centre de notifications</p><h1 className="text-2xl font-bold text-slate-950 dark:text-white">Notifications</h1><p className="mt-1 text-sm text-slate-500 dark:text-slate-400">Suivez les alertes internes : stock, ventes, factures, paiements, sauvegardes et securite.</p></div><div className="flex items-center gap-3"><span className="rounded-full bg-red-50 px-3 py-1 text-sm font-bold text-red-700 dark:bg-red-950 dark:text-red-200">{count} non lues</span><button onClick={()=>void patch('/notifications/read-all','Toutes les notifications sont marquees comme lues.')} className="rounded-md bg-brand-600 px-4 py-2 text-sm font-semibold text-white">Marquer tout comme lu</button></div></div>{message?<div className="rounded-lg border border-slate-200 bg-white p-4 text-sm dark:border-slate-800 dark:bg-slate-900">{message}</div>:null}<div className="grid gap-3 rounded-lg border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900 md:grid-cols-4"><select value={status} onChange={(e)=>setStatus(e.target.value)} className="rounded-md border border-slate-300 px-3 py-2 dark:border-slate-700 dark:bg-slate-950">{statusOptions.map((option)=><option key={option.value} value={option.value}>{option.label}</option>)}</select><select value={type} onChange={(e)=>setType(e.target.value)} className="rounded-md border border-slate-300 px-3 py-2 dark:border-slate-700 dark:bg-slate-950">{typeOptions.map((option)=><option key={option.value} value={option.value}>{option.label}</option>)}</select><input value={moduleName} onChange={(e)=>setModuleName(e.target.value)} placeholder="Module" className="rounded-md border border-slate-300 px-3 py-2 dark:border-slate-700 dark:bg-slate-950"/><button onClick={()=>void load()} className="rounded-md bg-slate-950 px-4 py-2 text-sm font-semibold text-white dark:bg-white dark:text-slate-950">Filtrer</button></div><div className="overflow-hidden rounded-lg border border-slate-200 bg-white shadow-sm dark:border-slate-800 dark:bg-slate-900">{loading?<div className="p-6 text-sm text-slate-500">Chargement...</div>:null}{!loading&&!items.length?<div className="p-6 text-sm text-slate-500">Aucune notification.</div>:null}{items.map((item)=><div key={item.id} className="flex flex-col justify-between gap-3 border-b border-slate-100 p-4 dark:border-slate-800 md:flex-row md:items-center"><div><div className="flex flex-wrap items-center gap-2"><span className={`rounded-full px-2 py-0.5 text-xs font-bold ${badgeClass(item.type)}`}>{labelType(item.type)}</span><span className="text-xs text-slate-500">{item.status === 'UNREAD' ? 'Non lue' : item.status === 'READ' ? 'Lue' : 'Archivee'}</span><span className="text-xs text-slate-400">{new Date(item.createdAt).toLocaleString('fr-HT')}</span></div><h2 className="mt-2 text-base font-bold text-slate-950 dark:text-white">{item.title}</h2><p className="mt-1 text-sm text-slate-600 dark:text-slate-300">{item.message}</p><p className="mt-1 text-xs text-slate-400">Module : {item.module??'general'} {item.referenceId?`- Ref. ${item.referenceId}`:''}</p></div><div className="flex gap-2"><button disabled={item.status==='READ'} onClick={()=>void patch(`/notifications/${item.id}/read`,'Notification marquee comme lue.')} className="rounded-md border border-slate-300 px-3 py-2 text-sm font-semibold disabled:opacity-50 dark:border-slate-700">Lu</button><button onClick={()=>void patch(`/notifications/${item.id}/archive`,'Notification archivee.')} className="rounded-md border border-slate-300 px-3 py-2 text-sm font-semibold dark:border-slate-700">Archiver</button></div></div>)}</div></div>}
function labelType(type:string){const value=type.toLowerCase();return value==='success'?'Succes':value==='warning'?'Alerte':value==='error'?'Erreur':'Info'}
function badgeClass(type:string){const value=type.toLowerCase();if(value==='success')return 'bg-emerald-50 text-emerald-700 dark:bg-emerald-950 dark:text-emerald-200';if(value==='warning')return 'bg-amber-50 text-amber-700 dark:bg-amber-950 dark:text-amber-200';if(value==='error')return 'bg-red-50 text-red-700 dark:bg-red-950 dark:text-red-200';return 'bg-sky-50 text-sky-700 dark:bg-sky-950 dark:text-sky-200'}