"use client";
import { FormEvent, useEffect, useMemo, useState } from "react";
import { getAccessToken } from "@/lib/auth";
type Permission={id:string;key:string;name:string;category:string;description?:string}; const apiUrl=process.env.NEXT_PUBLIC_API_URL??"http://localhost:3001";
export default function PermissionsSettingsPage(){const[permissions,setPermissions]=useState<Permission[]>([]);const[form,setForm]=useState({key:"",name:"",category:"",description:""});const[message,setMessage]=useState<string|null>(null);useEffect(()=>{loadPermissions();},[]);async function loadPermissions(){const response=await fetch(`${apiUrl}/permissions`,{headers:{Authorization:`Bearer ${getAccessToken()}`}});if(response.ok)setPermissions(await response.json());}async function handleSubmit(event:FormEvent<HTMLFormElement>){event.preventDefault();const response=await fetch(`${apiUrl}/permissions`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${getAccessToken()}`},body:JSON.stringify(form)});setMessage(response.ok?"Permission creee avec succes.":"Impossible de creer la permission.");if(response.ok){setForm({key:"",name:"",category:"",description:""});await loadPermissions();}}const grouped=useMemo(()=>permissions.reduce<Record<string,Permission[]>>((groups,permission)=>{groups[permission.category]=[...(groups[permission.category]??[]),permission];return groups;},{}),[permissions]);return <div className="grid gap-6 xl:grid-cols-[360px_1fr]"><section className="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><p className="text-sm font-medium text-brand-600">Paramètres</p><h1 className="mt-2 text-2xl font-bold text-slate-950 dark:text-white">Permissions</h1><form className="mt-5 space-y-4" onSubmit={handleSubmit}><input value={form.key} onChange={(e)=>setForm({...form,key:e.target.value})} placeholder="clé.permission" className="w-full rounded-md border border-slate-300 px-3 py-2 dark:border-slate-700 dark:bg-slate-950"/><input value={form.name} onChange={(e)=>setForm({...form,name:e.target.value})} placeholder="Nom" className="w-full rounded-md border border-slate-300 px-3 py-2 dark:border-slate-700 dark:bg-slate-950"/><input value={form.category} onChange={(e)=>setForm({...form,category:e.target.value})} placeholder="Catégorie" className="w-full rounded-md border border-slate-300 px-3 py-2 dark:border-slate-700 dark:bg-slate-950"/><textarea value={form.description} onChange={(e)=>setForm({...form,description:e.target.value})} placeholder="Description" className="min-h-24 w-full rounded-md border border-slate-300 px-3 py-2 dark:border-slate-700 dark:bg-slate-950"/><button className="w-full rounded-md bg-brand-600 px-4 py-2 text-sm font-semibold text-white hover:bg-brand-700">Créer la permission</button>{message?<p className="text-sm text-slate-600 dark:text-slate-300">{message}</p>:null}</form></section><section className="rounded-lg border border-slate-200 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><h2 className="text-lg font-semibold text-slate-950 dark:text-white">Liste des permissions</h2><div className="mt-4 grid gap-5 lg:grid-cols-2">{Object.entries(grouped).map(([category,items])=><div key={category}><p className="mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200">{category}</p><div className="space-y-2">{items.map((permission)=><div key={permission.id} className="rounded-md border border-slate-200 p-3 text-sm dark:border-slate-800"><p className="font-medium text-slate-950 dark:text-white">{permission.name}</p><p className="text-xs text-slate-500">{permission.key}</p></div>)}</div></div>)}</div></section></div>}
